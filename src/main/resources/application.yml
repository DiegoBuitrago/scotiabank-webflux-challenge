spring:
  r2dbc:
    url: r2dbc:h2:mem:///studentdb
    username: sa
    password:

  h2:
    console:
      enabled: true

  sql:
    init:
      mode: always

  server:
    error:
      include-message: always    